You can install all of these with pip install "fastapi[all]".
pip install fastapi

check document api otomatis 
http://127.0.0.1:8000/docs

run fast api uvicorn main:app --reload

pip install sqlalchemy

For this to work, you need to pip install python-dotenv.

jwt token pip install "python-jose[cryptography]"

password hasing pip install "passlib[bcrypt]"

catatan middleware
https://www.starlette.io/middleware/

ratelimited
pip install asgi-ratelimit[full]

tambah env cd to project

python -m venv env
python -m pip install --upgrade pip

env\Scripts\activate

jwt token

pip install "python-jose[cryptography]"
pip install "passlib[bcrypt]"